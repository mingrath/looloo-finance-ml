# Local-only credentials. Never commit .env.
ALPACA_API_KEY=
ALPACA_API_SECRET=
SEC_USER_AGENT="looloo-finance-ml contact@example.com"

# Required only for a canonical --live run. It must equal git rev-parse HEAD.
GIT_COMMIT=
